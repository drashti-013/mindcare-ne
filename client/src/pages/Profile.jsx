import React from 'react';import {UserRound,Languages,Phone,HeartHandshake,Mail,Stethoscope} from 'lucide-react';
export default function Profile({user}){return <div className="page"><div className="page-head"><div><span className="pill">{user.role==='doctor'?'Doctor Profile':user.role==='caregiver'?'Caregiver Profile':'Patient Profile'}</span><h1>{user.name}</h1><p>Personal details and care contacts.</p></div></div><div className="profile-grid"><div className="panel profile-card"><div className="avatar"><UserRound size={45}/></div><h2>{user.name}</h2><span className="role-chip">{user.role}</span><div className="details"><p><b>Age</b><span>{user.age||'Not added'}</span></p><p><Mail/><b>Email</b><span className="profile-contact"><a href={`mailto:${user.email}`}>{user.email}</a></span></p><p><Phone/><b>Phone</b><span className="profile-contact">{user.phone?<a href={`tel:${user.phone}`}>{user.phone}</a>:'Not added'}</span></p><p><Languages/><b>Language</b><span>{user.language||'English'}</span></p>{user.role==='doctor'&&<p><Stethoscope/><b>Specialization</b><span>{user.specialization||'Not added'}</span></p>}</div></div><div className="panel"><h3><HeartHandshake/> Emergency-ready</h3><p className="muted">Keep your contact details available so the care team can reach you when an SOS alert is triggered.</p><div className="sos-contact-list"><div className="sos-contact"><div><b>Email</b><small>{user.email||'Not added'}</small></div>{user.email&&<a className="text-link" href={`mailto:${user.email}`}><Mail size={15}/> Email</a>}</div><div className="sos-contact"><div><b>Phone number</b><small>{user.phone||'Not added'}</small></div>{user.phone&&<a className="text-link" href={`tel:${user.phone}`}><Phone size={15}/> Call</a>}</div></div><div className="note"><Phone size={15}/> Doctor and caregiver SOS notifications use these registered email and phone details.</div></div></div></div>}
